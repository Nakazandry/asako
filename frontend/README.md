# ASAKO Frontend

Interface React + Vite + Tailwind CSS pour ASAKO.

## Demarrage local

```bash
cp .env.example .env
npm install
npm run dev
```

Variable Vercel:

- `VITE_API_URL`: URL publique de l'API Render, par exemple `https://asako-api.onrender.com/api`
